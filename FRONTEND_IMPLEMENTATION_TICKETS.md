# Frontend Implementation Tickets

PRD 기준으로 `백엔드 제외, 프론트엔드만` 보았을 때 아직 남아 있는 구현 항목을 체크리스트 형태로 정리한 문서다.

사용 방법:

- 작업을 시작할 때 하위 체크박스를 먼저 채운다.
- 티켓 단위 구현이 끝나면 상위 체크박스를 체크한다.
- 티켓 완료 여부는 `화면 존재`, `상태 흐름 존재`, `사용자 액션 가능` 기준으로 판단한다.

---

## Priority 1

### [x] T1. 대시보드를 PRD형 활동 허브로 확장

목표:

- 현재 분석 이력 중심 대시보드를 `최근 활동 + 후속 행동 + 요약 허브`로 확장한다.

완료 기준:

- [x] 최근 분석 이력 섹션이 유지된다.
- [x] 최근 챗봇 활동 요약 카드가 추가된다.
- [x] 최근 커뮤니티 활동 요약 카드가 추가된다.
- [x] “다음 행동 추천” 또는 “권장 조치” 섹션이 추가된다.
- [x] 분석 결과 화면과 대시보드가 자연스럽게 연결된다.
- [x] 모바일에서도 레이아웃이 무너지지 않는다.

대상 파일:

- `src/app/components/Dashboard.tsx`
- `src/app/data/dashboard.ts`

---

### [x] T2. 챗봇 답변에 근거/출처 UI 추가

목표:

- 현재 대화 UI에 `근거 기반 답변 슬롯`을 추가해 PRD의 신뢰성 요구사항을 프론트 수준에서 반영한다.

완료 기준:

- [x] assistant 메시지 아래에 출처/근거 카드 UI를 넣을 수 있다.
- [x] 출처 카드에 `문서명`, `요약`, `유형` 같은 메타 정보를 표시할 수 있다.
- [x] 근거가 없을 때는 UI가 자연스럽게 숨겨진다.
- [x] 추천 질문 흐름과 충돌하지 않는다.
- [x] 모바일과 데스크톱 모두 가독성이 유지된다.

대상 파일:

- `src/app/components/Chatbot.tsx`
- `src/app/data/chatbot.ts`

---

### [x] T3. 정책/공공정보 전용 탐색 화면 추가

목표:

- 챗봇 외에도 정책/보증/지원제도를 탐색할 수 있는 전용 프론트 화면을 만들고, 카드/배너/모달을 통해 자연스럽게 유입되도록 설계한다.

완료 기준:

- [x] 새 라우트가 추가된다.
- [x] 정책 카드 리스트 또는 카테고리 탐색 UI가 있다.
- [x] 검색 또는 필터 UI가 있다.
- [x] 각 정책 카드에서 상세 요약을 확인할 수 있다.
- [x] 정책 상세는 전용 화면 또는 모달에서 확인할 수 있다.
- [x] 정책 화면에서 챗봇으로 이어지는 CTA가 있다.
- [x] 홈, 분석 결과, 챗봇, 대시보드 중 최소 2곳 이상에서 정책 카드/배너 형태의 유입 entry가 있다.
- [x] 토스트는 주요 정보 노출 수단이 아니라 보조 알림으로만 사용한다.
- [x] 기존 앱 스타일과 색 체계를 유지한다.

대상 파일:

- `src/app/routes.tsx`
- `src/app/components/` 아래 신규 정책 화면 컴포넌트
- 필요 시 `src/app/data/` 아래 정책 목업 데이터

구현 메모:

- 기본 구조는 `전용 화면 + 카드/배너/모달 유입`으로 간다.
- 광고성 팝업처럼 보이는 표현은 피하고, 추천 정보 카드나 상황 맥락형 배너로 설계한다.
- 토스트는 “새 정책이 있습니다” 같은 보조 알림에 한정한다.

---

## Priority 2

### [x] T4. 커뮤니티 신고 UX 강화

목표:

- 현재 토스트 수준인 신고 기능을 실제 사용자 흐름이 있는 프론트 UX로 확장한다.

완료 기준:

- [x] 신고 버튼 클릭 시 신고 다이얼로그 또는 시트가 열린다.
- [x] 신고 사유 선택 UI가 있다.
- [x] 추가 설명 입력란이 있다.
- [x] 제출 후 완료 상태가 명확히 표시된다.
- [x] 동일 게시물에 대한 중복 신고 UX를 고려한다.
- [x] 게시글 상세와 목록에서 모두 자연스럽게 접근 가능하다.

대상 파일:

- `src/app/components/Community.tsx`

---

### [x] T5. 매물 탐색 프론트 초안 추가

목표:

- PRD에 있는 매물 탐색 흐름을 프론트 수준에서 먼저 구현한다.

완료 기준:

- [x] 새 매물 탐색 라우트가 추가된다.
- [x] 지역, 가격, 유형 등 기본 필터 UI가 있다.
- [x] 매물 카드 리스트 UI가 있다.
- [x] 각 매물에 안전도/주의 배지 슬롯이 있다.
- [x] 매물 상세 또는 요약 모달/카드가 있다.
- [x] 계약서 분석/챗봇/커뮤니티와 연결되는 CTA가 있다.

대상 파일:

- `src/app/routes.tsx`
- `src/app/components/` 아래 신규 매물 탐색 컴포넌트
- 필요 시 `src/app/data/` 아래 목업 데이터

---

### [x] T6. 거주지 리뷰 정보 구조 정리 및 UI 설계

목표:

- PRD의 `후기/거주지 리뷰`를 현재 커뮤니티와 어떤 구조로 나눌지 프론트 기준으로 정리한다.

완료 기준:

- [x] 아래 두 방향 중 하나를 선택한다.
- [x] `커뮤니티 내부 탭으로 유지` 또는 `별도 리뷰 화면 분리`
- [x] 선택한 구조에 맞는 라우팅 또는 탭 UI가 구현된다.
- [x] 후기 데이터 카드에 지역/거주 형태/만족도 정보를 담을 수 있다.
- [x] 커뮤니티 글과 거주지 리뷰가 사용자 입장에서 혼동되지 않는다.

대상 파일:

- `src/app/components/Community.tsx`
- 또는 신규 리뷰 화면 컴포넌트

의사결정 메모:

- `별도 리뷰 화면 분리`로 결정했다. 커뮤니티는 질문/경고/토론, 리뷰는 실제 거주 후기라는 역할을 명확히 분리한다.

---

## Priority 3

### [x] T7. 관리자 화면 프론트 골격 추가

목표:

- PRD의 관리자 기능을 위한 최소 프론트 골격을 만든다.

완료 기준:

- [x] 관리자 전용 라우트가 추가된다.
- [x] 신고 목록/콘텐츠 관리/지식베이스 관리 탭이 보인다.
- [x] 게시글 신고 목록 더미 UI가 있다.
- [x] 콘텐츠 상태 배지와 기본 액션 버튼이 있다.
- [x] 일반 사용자 화면과 관리자 화면이 정보 구조상 분리된다.

대상 파일:

- `src/app/routes.tsx`
- `src/app/components/` 아래 신규 관리자 컴포넌트

---

### [x] T8. 관리자 운영 현황 대시보드 UI 추가

목표:

- 관리자 화면 안에서 운영 상태를 한눈에 볼 수 있는 요약 대시보드를 만든다.

완료 기준:

- [x] 신고 건수, 처리 대기 건수, 최근 활동 같은 요약 카드가 있다.
- [x] 지식베이스 최근 업데이트 상태를 보여주는 UI가 있다.
- [x] 운영 KPI 카드가 있다.
- [x] 관리자 화면 첫 진입점으로 충분한 정보 밀도를 가진다.

대상 파일:

- 관리자 관련 신규 컴포넌트

---

## Cross-Cutting Improvements

### [ ] X1. PRD 기능 연결 CTA 정리

목표:

- 각 기능 화면이 서로 단절되지 않도록 탐색 연결을 강화한다.

완료 기준:

- [ ] 분석 결과에서 챗봇/대시보드/정책 화면으로 이동 가능하다.
- [ ] 정책 화면에서 챗봇/매물 탐색으로 이동 가능하다.
- [ ] 매물 화면에서 분석/커뮤니티로 이동 가능하다.
- [ ] 커뮤니티에서 챗봇/정책 화면으로 이동 가능하다.

---

### [ ] X2. 프론트 목업 데이터 계층 확장

목표:

- 신규 화면도 기존처럼 `data/*` 경계로 목업 데이터를 분리해 이후 API 연동을 쉽게 만든다.

완료 기준:

- [ ] 신규 화면이 컴포넌트 내부 하드코딩 대신 `src/app/data/*`를 사용한다.
- [ ] 화면 렌더링 로직과 데이터 정의가 분리된다.
- [ ] 나중에 API adapter로 교체하기 쉬운 구조다.

---

## Suggested Execution Order

1. `T1` 대시보드 확장
2. `T2` 챗봇 근거 UI
3. `T3` 정책 탐색 화면
4. `T4` 커뮤니티 신고 UX
5. `T5` 매물 탐색 초안
6. `T6` 리뷰 구조 결정 및 UI
7. `T7` 관리자 골격
8. `T8` 관리자 운영 대시보드
9. `X1`, `X2` 마감 정리
