# Workflow Principles

이 문서는 `react-specialist.toml`, `frontend-developer.toml`, `ui-designer.toml`의 공통 원칙을 현재 프로젝트용 실행 규칙으로 요약한 문서다.

목적:

- 작업 전에 빠르게 기준을 확인한다.
- 기존 UI와 흐름을 불필요하게 깨지 않도록 한다.
- React/프론트/UI 작업을 한 가지 일관된 방식으로 수행한다.

---

## 1. 기본 원칙

- 기존 제품의 구조, UI 언어, 사용자 흐름을 기본값으로 존중한다.
- 변경은 항상 `가장 작은 안전한 수정`을 우선한다.
- 국소 문제를 해결할 때 불필요한 추상화나 대규모 리팩터링을 만들지 않는다.
- 사용자에게 보이는 동작과 상태 무결성을 함께 책임진다.

---

## 2. 작업 시작 전

작업 전에 반드시 먼저 확인할 것:

- 이 변경이 영향을 주는 `라우트`, `컴포넌트`, `상태`, `데이터 경계`
- 현재 사용자 흐름에서 어디가 시작점이고 어디가 종료점인지
- 기존 패턴을 그대로 재사용할 수 있는지
- 변경이 `로컬 수정`인지, `흐름 변화`인지

핵심 질문:

- 어떤 화면/경로를 고치는가?
- 어떤 상태가 바뀌는가?
- 어떤 기존 흐름이 영향을 받는가?
- 정말 이 범위만 바꾸면 되는가?

---

## 3. React 작업 원칙

- 체크리스트 처리처럼 하지 말고 실제 실행 경계와 상태 흐름을 먼저 본다.
- derived state, effect dependency, async update, stale closure 위험을 먼저 의심한다.
- controlled/uncontrolled input이 섞이지 않게 한다.
- conditional render 변경 시 `empty`, `loading`, `success`, `error` 상태를 함께 본다.
- 이벤트 핸들링은 명시적으로 두고, 숨은 부작용을 줄인다.
- 컴포넌트 책임과 상태 소유권이 흐려지지 않게 유지한다.

---

## 4. Frontend 구현 원칙

- diff는 작고 리뷰 가능해야 한다.
- 같은 경로의 동작만 고치고 주변 동작은 최대한 그대로 둔다.
- loading, empty, error 상태를 빠뜨리지 않는다.
- 키보드와 포커스 동작이 깨지지 않게 한다.
- 새 화면을 추가할 때는 기존 라우트와 탐색 흐름에 자연스럽게 연결한다.
- 목업 데이터가 필요하면 가능하면 `src/app/data/*` 경계로 분리한다.

---

## 5. UI 변경 원칙

- 새 UI는 기존 앱의 색, 표면감, 정보 밀도, 상호작용 리듬을 우선 따른다.
- 한 화면만 갑자기 다른 제품처럼 보이게 만들지 않는다.
- 비율 문제는 먼저 `로컬 컴포지션`으로 해결한다.
- 카드, 섹션, 패널 같은 고정 컨테이너는 높이를 함부로 바꾸지 않는다.
- 아이콘/텍스트/여백 비율은 공간 재분배로 먼저 해결하고, transform 보정은 마지막 수단으로 쓴다.
- 광고처럼 보이는 표현은 신뢰를 해칠 수 있으므로 정보성 UI에서는 피한다.

---

## 6. 기존 흐름 보호 규칙

- 새 기능은 기본적으로 기존 흐름 위에 `붙이는 방식`으로 설계한다.
- 기존 UI나 흐름을 바꿔야 하면 먼저 아래를 설명한다.

설명해야 하는 내용:

- 왜 이 수정이 필요한지
- 어떤 화면/흐름에 영향이 있는지
- 어떻게 자연스럽게 연결되는지

- 사용자가 명시적으로 원하지 않은 전체 톤 변경, 대규모 재설계는 하지 않는다.

---

## 7. 검증 원칙

작업 후 최소한 아래를 확인한다.

- 바뀐 정확한 사용자 경로 1개
- 실패 또는 경계 케이스 1개
- 통합 경계 1개

검증 예시:

- 화면이 정상 렌더링되는가
- 조건부 상태 전환 시 UI가 사라지거나 깨지지 않는가
- 입력 컴포넌트가 정상 동작하는가
- 라우팅/탭/모달/시트 연결이 자연스러운가
- 빌드가 통과하는가

가능하면 매 작업 후 `npm run build`로 확인한다.

---

## 8. 사용자 커뮤니케이션 원칙

- 작업 전에는 무엇을 이해했고 무엇을 먼저 볼지 짧게 알린다.
- 기존 흐름에 간섭이 있는 변경은 이유와 영향 범위를 먼저 설명한다.
- 자연스럽게 연결되는 작은 변경도 어떤 방식으로 붙일지 먼저 알린다.
- 실수로 방향을 잘못 잡았으면 바로 인정하고, 수정 기준을 다시 맞춘다.

---

## 9. 이 프로젝트에 특히 중요한 규칙

- 기존 앱 스타일과 색 체계는 유지한다.
- 새 설계가 기존 화면과 이질적으로 보이면 안 된다.
- 챗봇, 홈, 분석, 커뮤니티는 각각 독립적으로 예뻐 보이는 것보다 제품 전체로 자연스럽게 이어져야 한다.
- 정책/공공정보 기능은 `전용 화면 + 카드/배너/모달 유입`을 기본으로 한다.
- 관리자 기능이나 확장 기능을 추가하더라도 일반 사용자 핵심 흐름을 먼저 망치지 않는다.

---

## 10. 작업 전 체크리스트

- [ ] 변경 대상 라우트/컴포넌트/상태를 확인했다.
- [ ] 기존 사용자 흐름을 해치지 않는지 확인했다.
- [ ] 정말 작은 수정으로 가능한지 먼저 검토했다.
- [ ] loading/empty/error 상태 영향을 생각했다.
- [ ] 기존 디자인 언어를 유지하는지 확인했다.
- [ ] 변경이 흐름을 건드리면 이유와 영향 범위를 먼저 설명할 준비가 되어 있다.
- [ ] 검증할 성공 경로, 실패 경로, 통합 경계를 생각했다.

